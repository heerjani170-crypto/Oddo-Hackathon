PORT=5000
# MongoDB connection URI - Replace with your MongoDB Atlas string if needed.
# Falls back to local MongoDB default connection string if not specified.
MONGODB_URI=mongodb://localhost:27017/assetflow
